spring:
  ai:
    openai:
      enabled: false
    azure:
      openai:
        api-key: ${SPRING_AI_AZURE_API_KEY}
        endpoint: https://endpoint.openai.azure.com
        chat:
          options:
            deployment-name: gpt-5-mini
            temperature: 1.0
